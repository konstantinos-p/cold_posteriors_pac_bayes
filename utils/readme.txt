This folder contains utils that simplify testing a Laplace approximation model
as well as evaluating our PAC-Bayes bound for different values of the bound hyperparameters.

The folder also includes testing routines for the above utils.